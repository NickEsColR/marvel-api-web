import {BestCharacters} from "../components/BestCharacters";
import {ListLastEvent} from "../components/ListLastEvent";

export const Home = () => {
  return (
    <div>
      <BestCharacters />
      <ListLastEvent />
    </div>
  );
};
